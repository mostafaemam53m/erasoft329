ما هو Polymorphism؟
Polymorphism تعني "تعدد الأشكال"، وهي خاصية تسمح باستخدام نفس الواجهة (اسم الدالة) لتنفيذ سلوك مختلف بحسب الكائن (object) الذي يستدعيها.

بمعنى آخر: يمكننا استدعاء نفس الدالة على كائنات مختلفة، وكل كائن سينفذها بطريقة مختلفة تناسبه.


🔹 أنواع Polymorphism:
1. Compile-Time Polymorphism (أو Static Polymorphism):
يُعرف أيضًا باسم method overloading.

يحدث عندما يكون هناك أكثر من دالة بنفس الاسم ولكن بعدد أو نوع مختلف من المعاملات (parameters).

يتم تحديد أي دالة تُستدعى أثناء وقت الترجمة (compile time).



2. Run-Time Polymorphism (أو Dynamic Polymorphism):
يُعرف أيضًا باسم method overriding.

يحدث عندما تقوم الفئة الفرعية (subclass) بإعادة تعريف دالة موروثة من الفئة الأم.

يتم تحديد أي دالة تُستدعى أثناء وقت التشغيل (runtime).


🔹 لماذا نستخدم Polymorphism؟
المرونة في كتابة الكود.

إعادة استخدام الكود (Code Reusability).

سهولة التوسعة والتعديل.

دعم مفهوم الواجهات (Interfaces) و الوراثة (Inheritance).


 مثال واقعي مبسط:
تخيل أن لديك جهاز تحكم عن بعد (Remote Control). يمكن استخدامه لتشغيل:

التلفاز.

المكيف.

المروحة.

نفس الزر (مثلاً زر التشغيل) يؤدي وظيفة مختلفة حسب الجهاز. هذا هو Polymorphism في الحياة الواقعية.

